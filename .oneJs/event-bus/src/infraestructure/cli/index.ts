// import { DistributedEventBus } from '../bus/distributed-event-bus';
// import { LoggingMiddleware } from '../middleware/logging.middleware';
// import { UserService } from '../../application/services/UserService';

// Crear una instancia del EventBus
// const eventBus = new DistributedEventBus();

// Añadir middleware de logging
// eventBus.use(LoggingMiddleware);

// Crear el servicio de usuario
// const userService = new UserService(eventBus);

// Simular la creación de un usuario desde la CLI
// (async () => {
//   await userService.createUser('user-123');
// })();
